# Wednesday Frog Bot - Техническое описание проекта 🐸

## Обзор проекта

Wednesday Frog Bot — это полнофункциональный Telegram-бот для автоматической еженедельной отправки сгенерированных через ИИ изображений жабы с мемами про среду. Проект разработан с использованием современных практик Python-разработки и асинхронного программирования.

## Архитектура

### Общая структура

Проект следует принципам модульной архитектуры с четким разделением ответственности:

### Структура проекта

```
wednesday_tg_bot/
├── main.py                # Точка входа, супервизор режимов
├── bot/
│   ├── wednesday_bot.py   # Основной бот (боевой режим)
│   ├── support_bot.py     # Резервный бот (режим техработ)
│   └── handlers.py        # Обработчики команд
├── services/
│   ├── image_generator.py  # Генерация изображений (Kandinsky + dry-run)
│   ├── prompt_generator.py # Промпты через GigaChat
│   └── scheduler.py        # Планировщик задач
├── utils/
│   ├── config.py           # Конфигурация и валидация
│   ├── logger.py           # Логирование (loguru)
│   ├── chats_store.py      # Хранилище чатов
│   ├── usage_tracker.py    # Учёт ручных генераций и лимитов
│   ├── dispatch_registry.py# Антидубли рассылок
│   ├── models_store.py     # Список доступных моделей
│   ├── admins_store.py     # Дополнительные администраторы
│   └── metrics.py          # Метрики производительности
├── data/                   # JSON-хранилища (чаты, usage, dispatch, metrics, models, frogs/)
├── logs/                   # Логи с ротацией
├── requirements.txt
└── README.md
```

## Компоненты системы

### 1. Главный модуль (main.py)

**Ответственность:**
- Точка входа в приложение
- Graceful shutdown при получении сигналов (SIGINT, SIGTERM)
- Обработка ошибок запуска
- Валидация конфигурации

**Ключевые особенности:**
- `BotRunner` класс для управления жизненным циклом
- Retry механизм для запуска polling
- Безопасная обработка прерываний
- Очищает промежуточные состояния (`pending_*`) после каждого переключения, что предотвращает повторную отправку финальных сообщений и накопление устаревших ссылок на сообщения

### 2. Основной класс бота (bot/wednesday_bot.py)

**Ответственность:**
- Инициализация всех компонентов
- Регистрация обработчиков команд
- Управление планировщиком
- Отправка изображений в чаты

**Ключевые методы:**
- `setup_handlers()` — регистрация команд
- `setup_scheduler()` — настройка планировщика
- `send_wednesday_frog()` — основная функция отправки
- `start()` / `stop()` — управление жизненным циклом
- `on_my_chat_member()` — обработка добавления/удаления из чатов

**Особенности:**
- Автоматическое добавление чатов при добавлении бота
- Отправка приветственных сообщений
- Retry механизм с exponential backoff
- Обработка rate limits API Telegram
- Предотвращение дублирования отправок
- Последовательное редактирование статусного сообщения при переключении с SupportBot, исключающее дубли и учитывающее фактическую остановку/запуск

### 3. Обработчики команд (bot/handlers.py)

**Ответственность:**
- Обработка пользовательских команд
- Rate limiting для команды `/frog`
- Админ-команды

**Пользовательские команды:**
- `/start` — приветствие и основная информация
- `/help` — справка по пользовательским командам
- `/frog` — генерация жабы по запросу
- Обработка неизвестных команд

**Админ-команды:**
- `/status` — статус бота (dry-run проверки Kandinsky/GigaChat, метрики, текущие модели)
- `/force_send` — принудительная отправка
- `/add_chat <chat_id>` / `/remove_chat <chat_id>` — управление чатами
- `/list_chats` — список активных чатов
- `/set_kandinsky_model <pipeline_id|name>` — смена модели Kandinsky
- `/set_gigachat_model <model_name>` — смена модели GigaChat
- `/list_models` — доступные модели (обе системы)
- `/mod <user_id>` / `/unmod <user_id>` / `/list_mods` — управление администраторами
- `/set_frog_limit <threshold>` — настройка глобального лимита ручных `/frog` (ограничено квотой)
- `/set_frog_used <count>` — корректировка текущей месячной выработки `/frog`

**Rate limiting:**
- Per-user: 5 минут между запросами
- Global: 10 запросов в минуту
- Проверка лимитов через UsageTracker

### 4. Генератор изображений (services/image_generator.py)

**Ответственность:**
- Генерация изображений через Kandinsky API
- Обработка ошибок и retry
- Circuit breaker для защиты API

**Ключевые особенности:**
- **Circuit Breaker**:
  - Активируется при 5+ ошибках подряд
  - Cooldown: 5 минут
  - Автоматический reset
- **Retry механизм**:
  - Максимум 3 попытки
  - Exponential backoff
  - Обработка timeout
- **Промпты через GigaChat** с fallback на статические при недоступности API
- **Dry-run**: `check_api_status()` запрашивает `/pipelines` без траты генераций и сохраняет список моделей
- **Обработка изображений**:
  - Base64 декодирование
  - Валидация через Pillow
  - Локальное сохранение

**Метрики:**
- Количество успешных/неудачных генераций
- Среднее время генерации
- Количество retry
- Срабатывания circuit breaker

### 5. Планировщик задач (services/scheduler.py)

**Ответственность:**
- Планирование автоматических отправок
- Поддержка часовых поясов
- Проверка временных слотов

**Ключевые особенности:**
- **Многослотовая система**:
  - Несколько времен отправки в день
  - По умолчанию: 09:00, 12:00, 18:00
  - Настраивается через `SCHEDULER_SEND_TIMES`
- **Поддержка часовых поясов**:
  - Использует `zoneinfo`
  - По умолчанию: Europe/Moscow
  - Настраивается через `SCHEDULER_TZ`
- **Временной расчет**:
  - Проверка текущего времени vs целевого
  - Предотвращение повторного выполнения
  - Точность до минуты

**Типы задач:**
- `schedule_wednesday_task()` — еженедельная задача
- `schedule_daily_task()` — ежедневная задача
- `schedule_interval_task()` — интервальная задача
- `get_next_run()` — расчет следующего запуска

### 6. Конфигурация (utils/config.py)

**Ответственность:**
- Загрузка и валидация переменных окружения
- Парсинг настроек планировщика
- Константы для генерации изображений

**Основные классы:**
- `Config` — основная конфигурация (включая GigaChat и сертификаты)
- `ImageConfig` — настройки изображений
- `SchedulerConfig` — настройки планировщика

**Валидация:**
- Проверка обязательных переменных
- Парсинг времен отправки
- Валидация форматов (HH:MM)
- Проверка диапазонов значений

### 7. Логирование (utils/logger.py)

**Ответственность:**
- Настройка системы логирования
- Ротация и архивация логов
- Цветной вывод в консоль

**Особенности:**
- Использует `loguru`
- Ротация: 1 день
- Retention: 10 дней
- Compression: zip
- Цветной вывод в консоль
- Подробный формат с стеком вызовов

### 8. Хранилище чатов (utils/chats_store.py)

**Ответственность:**
- Сохранение списка активных чатов
- Добавление/удаление чатов
- Персистентность в JSON

**Формат данных:**
```json
{
  "chats": {
    "chat_id": {
      "title": "Chat Name"
    }
  }
}
```

### 9. Трекер использования (utils/usage_tracker.py)

**Ответственность:**
- Отслеживание генераций по месяцам
- Проверка лимитов
- Управление доступом к `/frog`

**Функции:**
- `increment(count)` — увеличить счетчик
- `get_month_total()` — получить текущий месяц
- `can_use_frog()` — проверка лимитов
- `get_limits_info()` — информация о лимитах

**Лимиты:**
- Monthly quota: 100 генераций
- Frog threshold: 70 генераций
- Значения квоты и порога хранятся в `usage_stats.json` (секция `settings`) и обновляются при вызове админ-команд

**Формат данных:**
```json
{
  "2025-01": {
    "count": 45
  }
}
```

### 10. Реестр отправок (utils/dispatch_registry.py)

**Ответственность:**
- Предотвращение дублирования отправок
- Отслеживание по тайм-слотам
- Очистка старых записей

**Формат ключа:**
`{date}_{time}:{chat_id}`

**Пример:**
`2025-01-08_12:00:123456789`

**Retention:** 7 дней

**Формат данных:**
```json
{
  "dispatches": {
    "2025-01-08_12:00:123456789": {
      "ts": "2025-01-08T09:00:00"
    }
  }
}
```

### 11. Метрики (utils/metrics.py)

**Ответственность:**
- Отслеживание производительности
- Статистика работы системы
- Health-check данные

**Метрики:**
- Генерации (успешные, всего, процент успеха)
- Время генерации (total, average)
- Circuit breaker trips
- Dispatch statistics

**Формат данных:**
```json
{
  "generations": {
    "success": 120,
    "failed": 5,
    "retries": 15,
    "total_time": 1200.5
  },
  "dispatches": {
    "success": 350,
    "failed": 2
  },
  "circuit_breaker_trips": 1
}
```

## Потоки данных

### 1. Запуск бота

```
main.py
  → BotRunner.__init__()
  → WednesdayBot.__init__()
    → Application.builder()
    → ImageGenerator.__init__()
    → TaskScheduler.__init__()
    → CommandHandlers.__init__()
  → setup_handlers()
  → setup_scheduler()
  → application.initialize()
  → application.start()
  → application.updater.start_polling()
  → scheduler.start() (async task)
  → while loop
```

### 2. Автоматическая отправка

```
Scheduler._check_wednesday_task()
  → Проверка времени
  → Если время пришло:
    → ImageGenerator.generate_frog_image()
      → _generate_image()
        → _get_pipeline_id()
        → _start_generation()
        → _wait_for_generation()
      → save_image_locally()
    → WednesdayBot.send_wednesday_frog()
      → Получение списка чатов
      → Для каждого чата:
        → Проверка DispatchRegistry
        → send_photo() с retry
        → Учет в UsageTracker
        → Метрики в Metrics
```

### 3. Команда /frog

```
User: /frog
  → CommandHandlers.frog_command()
    → Rate limit check
    → UsageTracker.can_use_frog()
    → ImageGenerator.generate_frog_image()
    → send_photo()
    → UsageTracker.increment()
    → Metrics update
```

### 4. Добавление в чат

```
Chat: added bot
  → ChatMemberHandler.on_my_chat_member()
    → ChatsStore.add_chat()
    → send_message() (welcome)
```

## Безопасность

### Защита от злоупотреблений

1. **Rate Limiting**:
   - Per-user: 5 минут
   - Global: 10/минуту

2. **Usage Tracking**:
   - Лимит 70 генераций в месяц для `/frog`
   - Автоматические отправки не ограничены

3. **Circuit Breaker**:
   - Защита от перегрузки API
   - Cooldown при множественных ошибках

4. **Админ-команды**:
   - Только для ADMIN_CHAT_ID
   - Проверка доступа

### Обработка ошибок

- Graceful degradation
- Retry с exponential backoff
- Timeout handling
- Логирование всех ошибок
- Circuit breaker для API

## Производительность

### Оптимизации

1. **Асинхронность**:
   - Все операции через asyncio
   - Параллельная отправка в чаты
   - Async HTTP запросы

2. **Connection Pooling**:
   - Переиспользование соединений
   - Keep-alive для API

3. **Resource Management**:
   - Локальное сохранение изображений
   - Очистка старых файлов
   - Retention политики

4. **Efficient Scheduling**:
   - Проверка каждые 30 секунд
   - Быстрая проверка временных слотов

## Масштабируемость

### Текущие ограничения

- Работает на одном сервере
- JSON-based хранение данных
- Прямое взаимодействие с Telegram API

### Возможные улучшения

- База данных для хранения
- Redis для кэширования
- Webhooks вместо polling
- Кластеризация для нагрузки
- Load balancing

## Мониторинг

### Метрики

- Количество генераций
- Success rate
- Average generation time
- Circuit breaker trips
- Active chats

### Логирование

- Все операции логируются
- Different уровни (INFO, WARNING, ERROR)
- Structured logging
- Traceback при ошибках

### Health Check

Команда `/status` (только админ) включает:
- Dry-run проверку Kandinsky (список моделей, текущая модель)
- Dry-run проверку GigaChat (получение токена, список моделей)
- Метрики и статус планировщика

## Тестирование

### Ручное тестирование

```bash
# Тестовый режим (запуск каждые N минут)
SCHEDULER_TEST_MINUTES=10 python main.py
```

### Мониторинг в продакшене

```bash
# Логи
tail -f logs/wednesday_bot_$(date +%Y-%m-%d).log

# Метрики
cat data/metrics.json | jq

# Использование
cat data/usage_stats.json | jq
```

## Деплой

### Требования

- Python 3.8+
- 100MB RAM минимум
- Доступ к интернету
- Persistence storage для данных

### Процесс деплоя

1. Клонирование репозитория
2. Установка зависимостей
3. Настройка `.env`
4. Проверка конфигурации
5. Запуск через systemd/launchd
6. Мониторинг логов

## Известные ограничения

1. **Синхронность данных**:
   - JSON файлы могут конфликтовать при высокой нагрузке
   - Нет транзакций

2. **Планировщик**:
   - Проверка каждые 30 секунд (не точное время)
   - Может пропустить выполнение при перезапуске

3. **Хранение изображений**:
   - Нет автоматической очистки
   - Максимум 30 файлов в папке

## Будущие улучшения

1. **База данных**:
   - PostgreSQL или SQLite
   - Миграции схемы

2. **Webhooks**:
   - Вместо polling
   - Lower latency

3. **Dashboard**:
   - Web интерфейс
   - Graphana для метрик

4. **Notifications**:
   - Email алерты
   - Telegram уведомления

---

**Версия:** 2.0
**Дата:** 2025
**Статус:** Production Ready 🚀
